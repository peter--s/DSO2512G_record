    // MOD: anchored to the left edge of the graticule (was 120 * menuMult), taking over the space the
    // status text vacated. The following two items keep their original spacing relative to it.
    const pos_timeDiv = gridLeftMargin + 4;
