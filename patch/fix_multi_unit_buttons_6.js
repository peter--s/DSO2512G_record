
    // Stock behaviour, deliberately kept: like led-multi and led-math1, this LED follows the app's
    // panel convention of "the adjacent control is live" - here, that the LEVEL knob does something
    // (it no-ops in AutoLvl). Trigger STATE is shown by the top-bar status text instead.
